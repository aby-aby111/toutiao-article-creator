---
name: toutiao-article-creator
description: This skill should be used when the user wants to create a Toutiao (今日头条) style article based on real-time social hot topics. Trigger when the user mentions "今日头条", "头条文章", "热点文章", "有什么热点", "最近热点", or asks to write a viral news article or browse hot topics. The skill can first list current hot topics for user selection, then searches for hot topic details, crafts the article following Toutiao's platform standards, and generates AI image prompts with precise placement instructions. Also supports automation mode for scheduled/cron tasks without user interaction.
---

# 今日头条文章创作者

## 概述

基于社会实时热点，创作符合今日头条平台规范的爆款文章，并生成 AI 配图提示词，明确标注每张图片的放置位置。

**快捷入口**：用户可以说"有什么热点"或"看看最近热点"，Skill 会自动搜索并罗列当前社会热点供用户选择。

**自动化模式**：本技能支持无用户交互的运行时环境（如自动化定时任务）。当上下文表明无法等待用户输入时（如自动化系统提示 "automation_trigger" 标识），自动进入自动化模式，跳过所有需要用户选择的步骤，全程自主决策完成创作。

## 自动化模式（无交互场景）

当检测到运行在自动化环境（如定时任务、cron、机器人推送）中，且用户不在线或无法交互时，使用此替代流程代替标准工作流程。

### 核心原则
- **无等待**：不输出列表让用户选择，不等待用户回复
- **自主决策**：AI 自行判断最佳热点、最佳标题、最佳配图
- **一次完成**：跳过所有分段确认，一步输出完整文章
- **质量优先**：如果没有合格的热点（搜索结果寥寥或热度极低），跳过本次执行，告知"今日没有合适的热点"

### 自动化流程

**第A1步：检测并筛选热点**
1. 使用 `web_search` 搜索多个来源的热点：
   - 搜索格式：`今日热搜 {当前日期（不含年份）}`
   - 搜索格式：`微博热搜 今日热点`
   - 搜索格式：`百度热搜 最新`
2. 从搜索结果中提炼热度最高或新闻价值最显著的 **1-2个热点**
3. **热度判定原则**：
   - 有明确热度数字（如"163万"）且 >50万的：合格
   - 重大国际/国内新闻事件（如元首访华、重大政策发布）：合格
   - 如果所有热点都模棱两可、无明确热度、无实质内容：判定为"无合格热点"，跳过本次执行
4. 自动选择其中最适合撰写长文的热点话题（优先选择有丰富信息量的严肃新闻/社会民生事件，其次选娱乐/科技话题）

**第A2步：搜索补充素材**
自动搜索所选热点相关的补充信息：
- 搜索格式：`{热点关键词} 最新进展 {当前日期}`
- 搜索格式：`{热点关键词} 数据`
- 搜索格式：`{热点关键词} 官方回应`

**第A3步：拟定并选择标题**
1. 拟定 **3个备选标题**，每个标题必须符合 20-30 字规范
2. 无需展示给用户，AI 直接选择**最具吸引力且最贴合内容**的一个作为最终标题
3. 执行标题自检：逐字数确认在20-30字之间

**第A4步：撰写正文+配图**
1. 直接根据选定的标题和收集的素材撰写完整文章
2. 严格遵循标准流程中的"手机排版铁律"（段落极短化、数据加粗、视觉分隔符、配图加密分布、有力结尾）
3. 文末附带结束标识

**第A5步：直接交付**
以对话形式直接输出完整文章，不做二次确认。

## 工作流程（交互模式）

### 第0步（可选）：热点发现与列表展示

**触发条件**：当用户说"有什么热点"、"最近有什么热点"、"看看今天的热点"、"推荐个热点"等，**先执行此步骤再继续**。如果用户已直接提供了热点主题，则跳过此步。

操作方式：
1. 使用 `web_search` 搜索当前社会热点，同时搜索多个来源以覆盖不同类型的热点：
   - 搜索格式：`今日热搜 {当前日期（不含年份）}`
   - 搜索格式：`微博热搜 今日热点`
   - 搜索格式：`百度热搜 最新`
   - 搜索格式：`微信热搜 今日`

2. 从搜索结果中提炼出 **8-12 个当前热点话题**，按类别整理为列表：

```
═══════════════════════════════════════
🔥 今日热点话题推荐

【社会民生】
1. {热点标题1} - 简述
2. {热点标题2} - 简述

【科技/财经】
3. {热点标题3} - 简述

【文娱/体育】
4. {热点标题4} - 简述

【国际】
5. {热点标题5} - 简述
...

═══════════════════════════════════════
请选择你感兴趣的热点编号或告诉我你想写什么话题：
```

3. **等待用户选择**其中一个热点或告知自己想写的话题，然后进入后续步骤。

### 第1步：接收热点主题

接收用户提供的热点关键词或短句。如果用户只提供了一个模糊的主题名，使用 `web_search` 搜索该主题的详细信息，获取：
- 事件最新进展
- 关键数据
- 多方观点
- 网友评论/争议点
- 时间线

### 第2步：搜索补充素材

使用 `web_search` 搜索热点相关的补充信息：
- 搜索格式：`{热点关键词} 最新进展 {当前日期}`
- 搜索格式：`{热点关键词} 数据`
- 搜索格式：`{热点关键词} 官方回应`
- 搜索格式：`{热点关键词} 网友评论`

**不直接向用户提问，直接搜索。**

### 第3步：拟定标题

参考 `references/toutiao-standards.md` 中的标题规范，拟定3个备选标题供用户选择。

**标题硬性规定（必须严格遵守）：**
- 字数：**20-30字**，超过30字直接重写，一个字都不能多
- 包含核心关键词（与事件强关联的词）
- 有吸引力的角度
- 不做标题党

**标题自检流程（写出后必须逐条核对）：**
- [ ] 字数是否在20-30字之间？（用脚标逐字数：1-2-3...）
- [ ] 是否包含核心事件关键词？
- [ ] 是否有"第1眼吸引力"？
- [ ] 是否避免了「震惊」「紧急通知」等标题党词？

向用户展示3个标题并让用户选择其一。

### 第4步：撰写正文

根据用户选择的标题和收集到的素材，撰写完整文章。

**🚀 手机排版铁律（必须严格遵守）：**

每条排版规则都有一个反面例子（❌）和一个正面例子（✅），看清楚再写。

**1. 段落极短化**
- 每段不超过 **2行**（以手机屏幕为准，约75字以内）
- 核心句可以 **独占一段**，起到强调作用
- ❌ 错误写法：300字不分段，一大坨文字
- ✅ 正确写法：一段只表达1个意思，写完立马换行

**2. 关键数据/金句加粗**
- 每个段落里的数据、时间、船名、关键引语用 `**加粗**`
- ❌ 错误写法：整段平铺，没有视觉落脚点
- ✅ 正确写法：**7万吨级巨轮**、"宏博3"轮、**12个小时**

**3. 善用视觉分隔符**
- 段落之间空行必须有
- 可以用特殊符号或引号做视觉锚点
- ✅ 可使用：**"** 引号作为段落开头的视觉抓手

**4. 配图加密分布**
- 配图数量从"每2-3段1张"改为 **"每1-2段插1张"**
- 配图数量规则：
  - 文章500-1000字：配 **4-5张图**
  - 文章1000字以上：配 **5-7张图**
- 配图位置：
  - 第1张：导语段 **正下方**
  - 第2~5张：**每1-2个短段落之间**均匀分布
  - 最后1张：**结尾互动段上方紧挨着**
- 图片位置标注紧跟在上方段落之后，**独立成行**

**5. 结尾要有力**
- 引导互动段独立成段，用 `【】` 标注
- 用反问句收尾，引导评论

### 第5步：生成配图提示词

根据 `references/image-prompt-guide.md` 的指南，为每张配图生成 AI 绘图提示词。

**配图数量规则：**
- 文章500-1000字：配4-5张图
- 文章1000字以上：配5-7张图

**配图分布规则：**
- 第1张：导语段正下方
- 中间图：每1-2个短段落之间均匀分布
- 最后1张：结尾互动段上方紧挨着

### 第6步：输出完整文章

以以下格式输出最终文章：

```
═══════════════════════════════════════
【标题备选】请选择你最喜欢的一个：

1. {标题1}
2. {标题2}
3. {标题3}
═══════════════════════════════════════
```

用户选择标题后，输出：

```
═══════════════════════════════════════
{选定的标题}

【导语】
{导语文字，不超过75字}
{续导语文字，不超过75字}

[图片位置: 导语段之后]
[配图提示词: {英文提示词}]

{正文短段落，不超过75字}

{正文短段落，不超过75字}

[图片位置: 第2段之后]
[配图提示词: {英文提示词}]

{正文短段落，不超过75字}

[图片位置: 第3段之后]
[配图提示词: {英文提示词}]

{正文短段落，不超过75字}

{正文短段落，不超过75字}

[图片位置: 第5段之后]
[配图提示词: {英文提示词}]

{正文短段落，不超过75字}

[图片位置: 结尾段之前]
[配图提示词: {英文提示词}]

【结尾】
{反问句} {引导讨论}

═══════════════════════════════════════
```

## 内容创作原则

1. **时效性**：确保文章内容与当前热点时间一致
2. **准确性**：所有数据标注来源，避免虚假信息
3. **平衡性**：多方观点兼顾，避免极端立场
4. **可读性**：手机端友好，段落短小精悍
5. **互动性**：结尾必须设置讨论引导

## 配图提示词语言规范

- 提示词使用英文（英文 AI 绘图模型效果更好）
- 提示词要包含：场景、主体、风格、构图、画质参数
- 具体格式参见 `references/image-prompt-guide.md`

## 注意事项

- 如果不确定热点事件的细节，使用 web_search 搜索确认，不要猜测
- 涉及敏感话题时保持客观中立，不煽动情绪
- 图片提示词须标注清楚放置在哪个段落之间的哪个位置
- 避免使用「据知情人士透露」「据传」等不确凿的来源
- 关注用户给出的反馈，必要时修改标题或调整内容

## 资源

### references/
- `toutiao-standards.md` —— 今日头条文章规范（标题、结构、配图、互动全指南）
- `image-prompt-guide.md` —— AI 配图提示词编写指南与位置标注规范
